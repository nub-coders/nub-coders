networks:
  web:
    external: true

services:
  nubcoders:
    container_name: nubcoders
    build:
      context: .
      dockerfile: Dockerfile
    image: nubcoders:latest
    restart: unless-stopped
    env_file:
      - .env
    environment:
      NODE_ENV: production
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.nubcoders.rule=Host(`nubcoders.com`) || Host(`www.nubcoders.com`)"
      - "traefik.http.routers.nubcoders.entrypoints=websecure"
      - "traefik.http.routers.nubcoders.tls.certresolver=letsencrypt"
      - "traefik.http.services.nubcoders.loadbalancer.server.port=8080"
    expose:
      - "8080"
    networks:
      - web
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M
        reservations:
          cpus: '0.25'
          memory: 256M
